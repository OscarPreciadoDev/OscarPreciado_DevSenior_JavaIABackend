## POO moderna + clases + objetos + encapsulamiento

    Se tocaran las bases de POO  

El metodo depende de un objeto para servir

Una funcion no depende de un objeto

Una variable dentro de POO se redefine como un ATRIBUTO

